# Default SQLite for local dev & Prisma CLI (db push / generate).
# Override in CI or production: set DATABASE_URL to your Postgres URL (deployment env wins over this file).
DATABASE_URL="file:./prisma/dev.db"
