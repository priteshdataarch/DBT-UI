version: 2

sources:
  - name: snowflake
    catalog: AwsDataCatalog
    schema: prod-users
    tables:

      - name: users
        description: users data from production

      - name: users
        description: users data from production
        columns:
        - name: user_id
          data_type: varchar
