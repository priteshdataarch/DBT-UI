version: 2

sources:
  - name: snowfale
    catalog: AwsDataCatalog
    schema: dbt
    tables:

      - name: sessions
        description: sessions data from production
        columns:
        - name: id
          data_type: string
          description: primary key
        - name: date
          data_type: timestamp
          description: execution date
