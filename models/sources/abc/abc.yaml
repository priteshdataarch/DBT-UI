version: 2

sources:
  - name: abc
    catalog: AwsDataCatalog
    schema: prod-raw
    tables:

      - name: abc
        description: abc data from production

      - name: ghj
        description: ghj data from production
        columns:
        - name: kd
          data_type: varchar

      - name: oop
        description: oop data from production

      - name: uv
        description: uv data from production
        columns:
        - name: id
          data_type: text
