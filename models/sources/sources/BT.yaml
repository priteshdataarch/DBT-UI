version: 2

sources:
  - name: sources
    catalog: AwsDataCatalog
    schema: AT
    tables:

      - name: users
        description: users data from production
        columns:
        - name: id
          data_type: varchar
        - name: role
          data_type: varchar
